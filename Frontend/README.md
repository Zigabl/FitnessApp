# React Native frontend

Minimal Expo + React Native + TypeScript login flow.

## Install

```bash
npm install
npx expo install react-native-screens react-native-safe-area-context
```

## Run

```bash
npm start
```

## Fake login

- E-mail: `test@example.com`
- Geslo: `password`

The fake authentication is in `src/features/auth/auth.api.ts` and can later be replaced by a real Express API call.
